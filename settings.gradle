pluginManagement {
    repositories {
        if (settings.hasProperty('customPortal')) {
            maven {
              url settings.customPortal
            }
        } else {
            gradlePluginPortal()
        }
    }
}